models:
  - type: main
    engine: nim
    model: meta/llama-3.1-70b-instruct
    parameters:
      base_url: http://llama31-70b-instruct-latency.runai-genai.svc.cluster.local/v1

  - type: self_check_input
    engine: nim
    model: meta/llama-3.1-8b-instruct
    parameters:
      base_url: http://llama31-8b-instruct-latency.runai-genai.svc.cluster.local/v1

knowledge_base:
  reranking_provider:
    name: default
    parameters:
      embedding_engine: nim
      embedding_model: nvidia/nv-rerankqa-mistral4b-v3
      base_url: http://mistral-4b-runai.runai-genai.svc.cluster.local/v1

rails:
  output:
    flows:
      - self check facts
      - add evidence

instructions:
  - type: general
    content: |
      You are a research bot that reply to user questions in a useful and truthful way.
